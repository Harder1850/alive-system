{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "http:default",
  "description": "Allow ALIVE UI to POST text input to local ui-bridge",
  "windows": ["main"],
  "permissions": [
    {
      "identifier": "http:default",
      "allow": [{ "url": "http://127.0.0.1:7331/*" }],
      "deny": []
    }
  ]
}

