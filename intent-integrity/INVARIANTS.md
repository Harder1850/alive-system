# INVARIANTS (Intent Integrity & Misuse Detection Stack)

This layer is advisory only

This layer does not block by default

This layer never enforces law or policy

This layer never claims authority

This layer always prefers clarification

This layer never assumes malicious intent

